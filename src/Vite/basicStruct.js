class basicStruct {
    constructor(provider) {
        this.currentProvider = provider;
    }

    setProvider(provider) {
        this.currentProvider = provider;
    }
}

export default basicStruct;
